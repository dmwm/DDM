#
# Sample configuration for the dashboard packages (covering global options).
# CREATED BY PUPPET

[dashboard]
logging.config.file     = /opt/dashboard/etc/dashboard-service-config/cmssw-listener/etc/logging.cfg

